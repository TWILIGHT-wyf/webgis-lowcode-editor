{
  "name": "lowcode-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -F @lowcode/editor dev",
    "build": "pnpm -F @lowcode/editor build",
    "lint": "eslint .",
    "type-check": "vue-tsc --noEmit"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "typescript": "^5.0.0",
    "vue-tsc": "^2.0.0",
    "prettier": "^3.0.0",
    "@types/node": "^20.0.0"
  }
}
