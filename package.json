{
  "name": "lowcode-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -F @vela/editor dev",
    "build": "pnpm -F @vela/editor build",
    "lint": "eslint .",
    "type-check": "vue-tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.9.3",
    "vue-tsc": "^2.2.12"
  }
}
