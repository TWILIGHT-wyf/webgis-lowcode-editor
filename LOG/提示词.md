这是一个低代码数据可视化搭建平台，做一个数字跳动、进度条、徽章组件，如果能用element plus库就调用，如果限制了自定义样式和属性就直接用原生的。调用组件时，组件会被存储到componentStore中，可以通过properties对组件属性进行修改，properties直接修改store，然后组件直接从store中获取更改，同步呈现在画布中的组件上，也可以通过useDataSource获取后端的数据。尽量多的样式和属性配置让用户高度自定义组件。在mockServer中写出用于测试获取数据的api。