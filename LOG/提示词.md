用element plus库做。调用组件时，组件会被存储到componentStore中，可以通过properties对组件属性进行修改，properties直接修改store，然后组件直接从store中获取更改，同步呈现在画布中的组件上，也可以通过useDataSource获取后端的数据。尽量多的样式和属性配置让用户高度自定义组件。在mockServer中写出用于测试获取数据的api。记得配置properties属性页面