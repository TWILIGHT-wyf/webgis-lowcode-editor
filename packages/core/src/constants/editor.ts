/**
 * 编辑器相关常量配置
 * 集中管理魔法数字，便于维护和调整
 */

// ============================================================================
// 视口与渲染
// ============================================================================

/** 视口裁剪边距（像素），提前渲染即将进入视口的组件，防止边界闪烁 */
export const VIEWPORT_PADDING = 200

/** 启用视口裁剪的组件数量阈值，低于此值直接渲染所有组件 */
export const VIEWPORT_CULLING_THRESHOLD = 50

// ============================================================================
// 拖拽交互
// ============================================================================

/** 拖拽触发阈值（像素），鼠标移动超过此距离才视为拖拽 */
export const DRAG_THRESHOLD = 5

/** 吸附触发阈值（像素），组件边缘距离小于此值时触发吸附 */
export const SNAP_THRESHOLD = 10

/** 网格吸附大小（像素） */
export const GRID_SIZE = 20

// ============================================================================
// 缩放限制
// ============================================================================

/** 最小缩放比例 */
export const MIN_SCALE = 0.1

/** 最大缩放比例 */
export const MAX_SCALE = 5

/** 缩放速度因子 */
export const ZOOM_FACTOR = 1.0015

// ============================================================================
// 历史记录
// ============================================================================

/** 历史记录最大栈深度 */
export const MAX_HISTORY_SIZE = 50

/** 历史记录防抖延迟（毫秒） */
export const HISTORY_DEBOUNCE_DELAY = 120

/** 历史记录节流间隔（毫秒） */
export const HISTORY_THROTTLE_INTERVAL = 200

// ============================================================================
// 组件尺寸限制
// ============================================================================

/** 组件最小宽度（像素） */
export const MIN_COMPONENT_WIDTH = 10

/** 组件最小高度（像素） */
export const MIN_COMPONENT_HEIGHT = 10
